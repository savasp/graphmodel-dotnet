== Cypher ==
MATCH (joined:Person)
MATCH (src)-[r:KNOWS]->(tgt)
WHERE r.__graphModelComplexProperty IS NULL AND r.GroupKey = joined.TestKey
RETURN {
                Node: joined,
                ComplexProperties: []
            } AS Node

== Parameters ==
(none)
