== Cypher ==
MATCH (joined:Person)
MATCH (src)-[r:KNOWS]->(tgt)
WHERE r.StartNodeId = joined.Id
RETURN {
                Node: joined,
                ComplexProperties: []
            } AS Node

== Parameters ==
(none)
