== Cypher ==
MATCH (joined:Person)
MATCH (src)-[r:KNOWS]->(tgt)
WHERE r.EndNodeId = joined.Id
RETURN {
                Node: joined,
                ComplexProperties: []
            } AS Node

== Parameters ==
(none)
