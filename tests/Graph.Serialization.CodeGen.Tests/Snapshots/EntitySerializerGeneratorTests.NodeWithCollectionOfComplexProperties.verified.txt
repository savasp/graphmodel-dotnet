== EntitySerializerRegistration.g.cs ==
// <auto-generated/>
#nullable enable

using System;
using Cvoya.Graph;
using Cvoya.Graph.Serialization;

internal static class EntitySerializerRegistration
{
    [System.Runtime.CompilerServices.ModuleInitializer]
    public static void Initialize()
    {
        EntitySerializerRegistry.Instance.Register<TestNamespace.Person>(new TestNamespace.Generated.PersonSerializer());
        EntitySerializerRegistry.Instance.Register(typeof(TestNamespace.PhoneNumber), new TestNamespace.Generated.PhoneNumberSerializer());
    }
}

== TestNamespace_PersonSerializer.g.cs ==
// <auto-generated/>
#nullable enable
#pragma warning disable CS1591 // Missing XML comments for publicly visible type or members

using System;
using System.Collections.Generic;
using System.Linq;
using Cvoya.Graph;
using Cvoya.Graph.Serialization;

namespace TestNamespace.Generated;

internal sealed class PersonSerializer : IEntitySerializer
{
    private readonly EntitySerializerRegistry _serializerRegistry = EntitySerializerRegistry.Instance;
    public Type EntityType => typeof(TestNamespace.Person);

    public object Deserialize(EntityInfo entity)
    {
        // Extracting init-only property 'Id'
        string idValue;
        // Look for property 'Id' in both simple and complex properties
        var idPropRep = entity.SimpleProperties.TryGetValue("Id", out var idSimpleProp) ? idSimpleProp
                    : entity.ComplexProperties.TryGetValue("Id", out var idComplexProp) ? idComplexProp : null;
        // Extract simple value - no conversion, just cast
        idValue = idPropRep is null
            ? Guid.NewGuid().ToString("N")
            : idPropRep.Value is SimpleValue idPropRepSimpleValue
                ? (string)idPropRepSimpleValue.Object
                : Guid.NewGuid().ToString("N");

        var result = new TestNamespace.Person(
        )
        {
            Id = idValue
        };

        // Set remaining properties with setters
        // Look for property 'FirstName' in both simple and complex properties
        var firstnamePropRep = entity.SimpleProperties.TryGetValue("FirstName", out var firstnameSimpleProp) ? firstnameSimpleProp
                    : entity.ComplexProperties.TryGetValue("FirstName", out var firstnameComplexProp) ? firstnameComplexProp : null;
        // Extract simple value - no conversion, just cast
        result.FirstName = firstnamePropRep is null
            ? string.Empty
            : firstnamePropRep.Value is SimpleValue firstnamePropRepSimpleValue
                ? (string)firstnamePropRepSimpleValue.Object
                : string.Empty;
        // Look for property 'PhoneNumbers' in both simple and complex properties
        var phonenumbersPropRep = entity.SimpleProperties.TryGetValue("PhoneNumbers", out var phonenumbersSimpleProp) ? phonenumbersSimpleProp
                    : entity.ComplexProperties.TryGetValue("PhoneNumbers", out var phonenumbersComplexProp) ? phonenumbersComplexProp : null;
        // Extract collection value
        result.PhoneNumbers = phonenumbersPropRep is null
            ? new System.Collections.Generic.List<TestNamespace.PhoneNumber>()
            : phonenumbersPropRep.Value is EntityCollection phonenumbersPropRepEntityCollection
                ? phonenumbersPropRepEntityCollection.Entities
                    .Select((entityItem, index) => (_serializerRegistry.GetSerializer(entityItem.ActualType)
                        ?? _serializerRegistry.GetSerializer(typeof(TestNamespace.PhoneNumber))
                        ?? throw new InvalidOperationException($"No serializer found for element type {entityItem.ActualType}"))
                        .Deserialize(entityItem) switch
                        {
                            TestNamespace.PhoneNumber typedElement => typedElement,
                            null => throw new GraphException($"Complex collection property 'PhoneNumbers' contains a null element at index {index}, but its target element type '{typeof(TestNamespace.PhoneNumber)}' does not allow null elements."),
                            var untypedElement => throw new GraphException($"Complex collection property 'PhoneNumbers' contains an element of type '{untypedElement.GetType()}' at index {index}, which is not assignable to its target element type '{typeof(TestNamespace.PhoneNumber)}'."),
                        })
                    .ToList()
                : throw new InvalidOperationException("Required collection property 'PhoneNumbers' is missing or null");
        // Look for property 'Labels' in both simple and complex properties
        var labelsPropRep = entity.SimpleProperties.TryGetValue("Labels", out var labelsSimpleProp) ? labelsSimpleProp
                    : entity.ComplexProperties.TryGetValue("Labels", out var labelsComplexProp) ? labelsComplexProp : null;
        // Extract collection value
        result.Labels = labelsPropRep is null
            ? new System.Collections.Generic.List<string>()
            : labelsPropRep.Value is SimpleCollection labelsPropRepSimpleCollection
                ? labelsPropRepSimpleCollection.Values
                    .Select((simpleValue, index) => simpleValue.Object is null
                        ? throw new GraphException($"Collection property 'Labels' contains a null element at index {index}, but its target element type '{typeof(string)}' is non-nullable.")
                        : (string)simpleValue.Object)
                    .ToList()
                : throw new InvalidOperationException("Required collection property 'Labels' is missing or null");
        return result;
    }

    public EntityInfo Serialize(object obj)
    {
        var entity = (TestNamespace.Person)obj;
        var simpleProperties = new Dictionary<string, Property>();
        var complexProperties = new Dictionary<string, Property>();

        // Serialize property: FirstName
        {
            var propInfo = typeof(TestNamespace.Person).GetProperty("FirstName")!;
            var value = entity.FirstName;
            Serialized? serializedValue;

            serializedValue = value is null
                ? null
                : new SimpleValue(
                    Object: value,
                    Type: typeof(string)
                );

            var propertyRep = new Property(
                PropertyInfo: propInfo,
                Label: "FirstName",
                IsNullable: false,
                Value: serializedValue
            );
            simpleProperties["FirstName"] = propertyRep;
        }

        // Serialize property: PhoneNumbers
        {
            var propInfo = typeof(TestNamespace.Person).GetProperty("PhoneNumbers")!;
            var value = entity.PhoneNumbers;
            Serialized? serializedValue;

            serializedValue = value is null
                ? null
                : new EntityCollection(
                    Type: typeof(TestNamespace.PhoneNumber),
                    Entities: value
                        .Select((item, index) => item is null
                            ? throw new GraphException($"Complex collection property 'PhoneNumbers' contains a null element at index {index}, but its target element type '{typeof(TestNamespace.PhoneNumber)}' does not allow null elements.")
                            : (_serializerRegistry.GetSerializer(item.GetType())
                                ?? _serializerRegistry.GetSerializer(typeof(TestNamespace.PhoneNumber))
                                ?? throw new InvalidOperationException($"No serializer found for type {item.GetType()}"))
                                .Serialize(item))
                        .ToList()
                );

            var propertyRep = new Property(
                PropertyInfo: propInfo,
                Label: "PhoneNumbers",
                IsNullable: false,
                Value: serializedValue,
                RelationshipType: GraphDataModel.GetComplexPropertyRelationshipType(propInfo)
            );
            complexProperties["PhoneNumbers"] = propertyRep;
        }

        // Serialize property: Id
        {
            var propInfo = typeof(Cvoya.Graph.Node).GetProperty("Id")!;
            var value = entity.Id;
            Serialized? serializedValue;

            serializedValue = value is null
                ? null
                : new SimpleValue(
                    Object: value,
                    Type: typeof(string)
                );

            var propertyRep = new Property(
                PropertyInfo: propInfo,
                Label: "Id",
                IsNullable: false,
                Value: serializedValue
            );
            simpleProperties["Id"] = propertyRep;
        }

        // Serialize property: Labels
        {
            var propInfo = typeof(Cvoya.Graph.Node).GetProperty("Labels")!;
            var value = entity.Labels;
            Serialized? serializedValue;

            serializedValue = value is null
                ? null
                : new SimpleCollection(
                    Values: value
                        .Select(item => new SimpleValue(item!, typeof(string)))
                        .ToList(),
                    ElementType: typeof(string)
                );

            var propertyRep = new Property(
                PropertyInfo: propInfo,
                Label: "Labels",
                IsNullable: false,
                Value: serializedValue
            );
            simpleProperties["Labels"] = propertyRep;
        }

        var actualLabels = entity.Labels?.ToList() ?? new List<string>();
        var primaryLabel = actualLabels.Count > 0 ? actualLabels[0] : "Person";
        return new EntityInfo(
            ActualType: typeof(TestNamespace.Person),
            Label: primaryLabel,
            ActualLabels: actualLabels,
            SimpleProperties: simpleProperties,
            ComplexProperties: complexProperties
        );
    }

    [ThreadStatic]
    private static HashSet<Type>? _currentlyProcessing;
    private static readonly System.Collections.Concurrent.ConcurrentDictionary<Type, EntitySchema> _schemaCache = new();

    /// <summary>
    /// Gets the schema information for Person.
    /// </summary>
    public EntitySchema GetSchema()
    {
        return PersonSerializer.GetSchemaStatic();
    }

    /// <summary>
    /// Gets the schema information for Person.
    /// </summary>
    public static EntitySchema GetSchemaStatic()
    {
        var typeKey = typeof(TestNamespace.Person);
        var currentlyProcessing = _currentlyProcessing ??= new HashSet<Type>();

        // Return cached schema if available
        if (_schemaCache.TryGetValue(typeKey, out var cachedSchema))
            return cachedSchema;

        // Check if we're already processing this type (circular reference)
        if (currentlyProcessing.Contains(typeKey))
        {
            // Return a schema without nested schemas to break the cycle
            return new EntitySchema(
                ExpectedType: typeof(TestNamespace.Person),
                Label: "Person",
                IsNullable: true,
                IsSimple: false,
                SimpleProperties: new Dictionary<string, PropertySchema>(),
                ComplexProperties: new Dictionary<string, PropertySchema>()
            );
        }

        // Mark this type as being processed
        currentlyProcessing.Add(typeKey);

        try
        {
            // Build the actual schema
            var simpleProperties = new Dictionary<string, PropertySchema>();
            var complexProperties = new Dictionary<string, PropertySchema>();
        // Schema for property: Labels
        {
            var propInfo = typeof(TestNamespace.Person).GetProperty("Labels")
                ?? typeof(Cvoya.Graph.INode).GetProperty("Labels")!;
            simpleProperties["Labels"] = new PropertySchema(
                PropertyInfo: propInfo,
                PropertyName: "Labels",
                PropertyType: PropertyType.SimpleCollection,
                ElementType: typeof(string),
                IsNullable: false
            ) { IsElementNullable = false };
        }

        // Schema for property: Id
        {
            var propInfo = typeof(TestNamespace.Person).GetProperty("Id")
                ?? typeof(Cvoya.Graph.IEntity).GetProperty("Id")!;
            simpleProperties["Id"] = new PropertySchema(
                PropertyInfo: propInfo,
                PropertyName: "Id",
                PropertyType: PropertyType.Simple,
                IsNullable: false
            );
        }

        // Schema for property: FirstName
        {
            var propInfo = typeof(TestNamespace.Person).GetProperty("FirstName")!;
            simpleProperties["FirstName"] = new PropertySchema(
                PropertyInfo: propInfo,
                PropertyName: "FirstName",
                PropertyType: PropertyType.Simple,
                IsNullable: false
            );
        }

        // Schema for property: PhoneNumbers
        {
            var propInfo = typeof(TestNamespace.Person).GetProperty("PhoneNumbers")!;
            complexProperties["PhoneNumbers"] = new PropertySchema(
                PropertyInfo: propInfo,
                PropertyName: "PhoneNumbers",
                PropertyType: PropertyType.ComplexCollection,
                ElementType: typeof(TestNamespace.PhoneNumber),
                IsNullable: false,
                NestedSchema: TestNamespace.Generated.PhoneNumberSerializer.GetSchemaStatic(),
                RelationshipType: GraphDataModel.GetComplexPropertyRelationshipType(propInfo)
            );
        }


            var schema = new EntitySchema(
                ExpectedType: typeof(TestNamespace.Person),
                Label: "Person",
                IsNullable: true,
                IsSimple: false,
                SimpleProperties: simpleProperties,
                ComplexProperties: complexProperties
            );

            // Cache the complete schema
            return _schemaCache.GetOrAdd(typeKey, schema);
        }
        finally
        {
            // Remove from processing set
            currentlyProcessing.Remove(typeKey);
        }
    }
}

== TestNamespace_PhoneNumberSerializer.g.cs ==
// <auto-generated/>
#nullable enable
#pragma warning disable CS1591 // Missing XML comments for publicly visible type or members

using System;
using System.Collections.Generic;
using System.Linq;
using Cvoya.Graph;
using Cvoya.Graph.Serialization;

namespace TestNamespace.Generated;

internal sealed class PhoneNumberSerializer : IEntitySerializer
{
    private readonly EntitySerializerRegistry _serializerRegistry = EntitySerializerRegistry.Instance;
    public Type EntityType => typeof(TestNamespace.PhoneNumber);

    public object Deserialize(EntityInfo entity)
    {

        var result = new TestNamespace.PhoneNumber(
        );

        // Set remaining properties with setters
        // Look for property 'CountryCode' in both simple and complex properties
        var countrycodePropRep = entity.SimpleProperties.TryGetValue("CountryCode", out var countrycodeSimpleProp) ? countrycodeSimpleProp
                    : entity.ComplexProperties.TryGetValue("CountryCode", out var countrycodeComplexProp) ? countrycodeComplexProp : null;
        // Extract simple value - no conversion, just cast
        result.CountryCode = countrycodePropRep is null
            ? string.Empty
            : countrycodePropRep.Value is SimpleValue countrycodePropRepSimpleValue
                ? (string)countrycodePropRepSimpleValue.Object
                : string.Empty;
        // Look for property 'Number' in both simple and complex properties
        var numberPropRep = entity.SimpleProperties.TryGetValue("Number", out var numberSimpleProp) ? numberSimpleProp
                    : entity.ComplexProperties.TryGetValue("Number", out var numberComplexProp) ? numberComplexProp : null;
        // Extract simple value - no conversion, just cast
        result.Number = numberPropRep is null
            ? string.Empty
            : numberPropRep.Value is SimpleValue numberPropRepSimpleValue
                ? (string)numberPropRepSimpleValue.Object
                : string.Empty;
        return result;
    }

    public EntityInfo Serialize(object obj)
    {
        var entity = (TestNamespace.PhoneNumber)obj;
        var simpleProperties = new Dictionary<string, Property>();
        var complexProperties = new Dictionary<string, Property>();

        // Serialize property: CountryCode
        {
            var propInfo = typeof(TestNamespace.PhoneNumber).GetProperty("CountryCode")!;
            var value = entity.CountryCode;
            Serialized? serializedValue;

            serializedValue = value is null
                ? null
                : new SimpleValue(
                    Object: value,
                    Type: typeof(string)
                );

            var propertyRep = new Property(
                PropertyInfo: propInfo,
                Label: "CountryCode",
                IsNullable: false,
                Value: serializedValue
            );
            simpleProperties["CountryCode"] = propertyRep;
        }

        // Serialize property: Number
        {
            var propInfo = typeof(TestNamespace.PhoneNumber).GetProperty("Number")!;
            var value = entity.Number;
            Serialized? serializedValue;

            serializedValue = value is null
                ? null
                : new SimpleValue(
                    Object: value,
                    Type: typeof(string)
                );

            var propertyRep = new Property(
                PropertyInfo: propInfo,
                Label: "Number",
                IsNullable: false,
                Value: serializedValue
            );
            simpleProperties["Number"] = propertyRep;
        }

        return new EntityInfo(
            ActualType: typeof(TestNamespace.PhoneNumber),
            Label: "PhoneNumber",
            ActualLabels: new List<string>(),
            SimpleProperties: simpleProperties,
            ComplexProperties: complexProperties
        );
    }

    [ThreadStatic]
    private static HashSet<Type>? _currentlyProcessing;
    private static readonly System.Collections.Concurrent.ConcurrentDictionary<Type, EntitySchema> _schemaCache = new();

    /// <summary>
    /// Gets the schema information for PhoneNumber.
    /// </summary>
    public EntitySchema GetSchema()
    {
        return PhoneNumberSerializer.GetSchemaStatic();
    }

    /// <summary>
    /// Gets the schema information for PhoneNumber.
    /// </summary>
    public static EntitySchema GetSchemaStatic()
    {
        var typeKey = typeof(TestNamespace.PhoneNumber);
        var currentlyProcessing = _currentlyProcessing ??= new HashSet<Type>();

        // Return cached schema if available
        if (_schemaCache.TryGetValue(typeKey, out var cachedSchema))
            return cachedSchema;

        // Check if we're already processing this type (circular reference)
        if (currentlyProcessing.Contains(typeKey))
        {
            // Return a schema without nested schemas to break the cycle
            return new EntitySchema(
                ExpectedType: typeof(TestNamespace.PhoneNumber),
                Label: "PhoneNumber",
                IsNullable: true,
                IsSimple: false,
                SimpleProperties: new Dictionary<string, PropertySchema>(),
                ComplexProperties: new Dictionary<string, PropertySchema>()
            );
        }

        // Mark this type as being processed
        currentlyProcessing.Add(typeKey);

        try
        {
            // Build the actual schema
            var simpleProperties = new Dictionary<string, PropertySchema>();
            var complexProperties = new Dictionary<string, PropertySchema>();
        // Schema for property: CountryCode
        {
            var propInfo = typeof(TestNamespace.PhoneNumber).GetProperty("CountryCode")!;
            simpleProperties["CountryCode"] = new PropertySchema(
                PropertyInfo: propInfo,
                PropertyName: "CountryCode",
                PropertyType: PropertyType.Simple,
                IsNullable: false
            );
        }

        // Schema for property: Number
        {
            var propInfo = typeof(TestNamespace.PhoneNumber).GetProperty("Number")!;
            simpleProperties["Number"] = new PropertySchema(
                PropertyInfo: propInfo,
                PropertyName: "Number",
                PropertyType: PropertyType.Simple,
                IsNullable: false
            );
        }


            var schema = new EntitySchema(
                ExpectedType: typeof(TestNamespace.PhoneNumber),
                Label: "PhoneNumber",
                IsNullable: true,
                IsSimple: false,
                SimpleProperties: simpleProperties,
                ComplexProperties: complexProperties
            );

            // Cache the complete schema
            return _schemaCache.GetOrAdd(typeKey, schema);
        }
        finally
        {
            // Remove from processing set
            currentlyProcessing.Remove(typeKey);
        }
    }
}
