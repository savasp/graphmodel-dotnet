== Cypher ==
MATCH (joined:Person)
MATCH (src)-[r:KNOWS]->(tgt)
WHERE tgt.Id = joined.Id
RETURN {
                Node: joined,
                ComplexProperties: []
            } AS Node

== Parameters ==
(none)
